{
  "items": [
<% @cdh_service_config.each_with_index do |config, i| %>
  {
    "name": "<%= config['name'] %>",
    "value": "<%= config['value'] %>"
  }<%= ',' if i < ( @cdh_service_config.size - 1) %>
<% end %>
  ]
}

