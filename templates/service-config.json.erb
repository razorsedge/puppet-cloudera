{
  "items": [
<% @cdh_items_config.each_with_index do |config, i| %>
  {
    "name": "<%= config['name'] %>",
    "value": "<%= config['value'] %>"
  }<%= ',' if i < ( @cdh_items_config.size - 1) %>
<% end %>
  ]
}

