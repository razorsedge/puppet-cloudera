{
  "items": [
<% @cdh_service_roles.each_with_index do |role, i| %>
  {
    "name": "<%= @hostname %>-<%= role %>",
    "type": "<%= role %>",
    "hostRef": { "hostId": "<%= @fqdn %>" }
  }<%= ',' if i < ( @cdh_service_roles.size - 1) %>
<% end %>
  ]
}
